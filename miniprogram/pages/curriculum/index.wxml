<view class="page"><view class="eyebrow">YOUR BOOKS</view><view class="title">跟课本学</view><view wx:for="{{books}}" wx:key="id" wx:for-item="book"><view class="section">{{book.name}}</view><navigator class="card" wx:for="{{book.units}}" wx:key="id" url="/pages/unit-detail/index?curriculumId={{book.id}}&amp;unitId={{item.id}}"><view class="tag">UNIT {{index + 1}}</view><view class="card-title">{{item.name}} →</view><view class="muted">{{item.entries.length}} 个词条</view></navigator></view></view>
