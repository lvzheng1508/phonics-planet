{
  "name": "phonics-planet",
  "version": "0.1.0",
  "private": true,
  "description": "拼读星球开发骨架",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:data": "node scripts/build-data.cjs",
    "validate:data": "node scripts/validate-data.cjs",
    "test": "node --test tests/*.test.cjs",
    "check": "npm run validate:data && npm run build:data && npm test && node scripts/check-project.cjs"
  }
}
