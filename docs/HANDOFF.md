# 开发交接 · 2026-09-08

## 最新纠偏：音标必须成为词卡核心

用户指出仅有单词释义卡偏离音标学习目标，现已补入145条待核对IPA并在详情/列表/复习显示。不要再因为整词录音缺失而把音标全部隐藏。详情提供可点击的音素入口；6条IPA有直接词典证据，其余139条AI草稿，全部pending，详见IPA-SOURCES.md。

用户授权本地系统英式整词试听。浏览器服务以macOS Daniel按需生成WAV，音频不入Git，只在内存注入preview资产。正式配置allowPreviewAudio=false。AudioService新增真实currentTime/duration/progress，详情和复习渐变随整词时钟变化；未实现精确音素时间对齐，不能声称已经完成。

当前32项测试及11页模板编译通过。浏览器实测gingerbread house显示 /ˈdʒɪndʒəbred ˌhaʊs/，系统文件时长1.410884秒，播放过程中音标着色宽度变化，结束归零。原生真机仍待验。以下第一轮记录是此前基线。

## 当前状态

第一轮本地学习流程已实现，分支 `feat/learning-loop`。原仓库位于 `/Users/lvzheng/cursor/phonics-planet`，本轮未推送远程。实现过程使用独立工作树，最终提交后快进同步回原仓库。

- 首页、教材/单元、音素总表/详情、单词详情、我的单词、今日学习、复习、练习、来源说明，共 11 页。
- StorageService 使用 `phonics-planet:user:v2`，从旧收藏键只读迁移。读失败可显示空态，但任何写入必须先成功读取，避免覆盖旧数据；写失败不推进评分。
- ContentService 统一搜索、教材语境及审核状态；支持单元 pronunciationId 指向独立变体，未找到变体不回退为其他读音。
- LearningService 生成真实今日计数与到期收藏队列；复习间隔 1/3/7/14 天，“再练练”重置为次日。
- PracticeService 实现三类练习的生成与评分，排除未审核内容和同形异音歧义；听音题播放完成后才可作答。当前真实数据不满足出题条件，显示空态。
- AudioService 提供 owner、状态和片段索引，清理旧上下文，忽略迟到事件，处理错误与超时。没有引入虚假音频。

## 验证记录

- `TZ=Asia/Shanghai npm run check`：26 个行为测试通过，JS/JSON 检查及 11 页 WCC/WCSC 编译通过。
- 浏览器预览使用实际控制器与 WXML 编译输出；手动验证自定义 starfish 添加/保存、收藏到翻卡复习、显示次日日期、完成计数刷新，以及单元搜索与缺失音频禁用。
- 独立只读审查发现临时读失败覆盖本地数据的问题，已修复并增加回归测试；补测了听音答题门槛、保存失败重试、重复答案、完成计分和同类音唯一答案。
- `node scripts/content-status.cjs --release` 如预期退出 1：145 个 Word、已审核发音 0、可播放整词 0、可播放音素 0、教材 pending。
- **未完成微信开发者工具及真机验证**。浏览器适配层只用于开发检查，不能验证原生生命周期、音频格式或真机排版；录音缺失也无法真实试听完整学习链路。

## 下一步所需内容

用户已明确选择许可清楚的公开音频优先。12 个调查候选位于 `seed-data/audio-candidates.json`，6 个带 IPA 来源的 AI 拼读候选位于 `seed-data/learning-samples.json`，均不导入运行时。公开媒体下载连接被重置，候选均未取得或试听；详情见 AUDIO.md。不要把候选地址直接升级为 verified。

继续核验词库与多音词语境，取得许可明确的英式单词/音素录音，记录文件校验值与审核人。审核后才能填充正式 words/audio-manifest 并启用音标、逐音与练习。缺少的单音或整词再安排录制。后续开发由 AI 根据状态自行规划，无需重新搭建页面。

## 本地命令

`npm ci` 安装开发依赖；`npm run check` 完整检查；`npm run preview` 启动 http://127.0.0.1:4173 。预览仅监听本机，记录独立保存在浏览器，不写入微信用户数据。
