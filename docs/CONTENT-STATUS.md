# 内容与素材状态 · 2026-09-08

## 当前音频交付（2026-09-12）

新增完整 Emma 下载包：145 条当前整词、ZIP 2,265,358 字节，资源路径 `bundles/audio/words-8622c24fb2d33504.zip`。下载页安装后本地播放，缺失音素不包含在包内，全部审核状态保持不变。

已按用户选定的 A · Emma（Kokoro bf_emma、speed 0.8）生成 145 份 WAV 与 MP3，MP3 共 2,356,317 字节。MP3 已实际写入 resource 的 `audio/words/en-GB/kokoro-emma/`（136 条使用 v1，9 条使用 v2-aligned），资源仓库与应用种子清单均已更新。六单元覆盖为 26/26、25/25、20/20、17/17、29/29、29/29，run 跨单元共享。

145 份整词音频均为 synthetic-preview / pending，44 个音素仍 missing。`npm run check` 通过 62 项测试及 13 页编译；147 项本地资源（145 整词 + 2 技术样本）校验通过。资源已推送到 `669f159`，当前清单全部 147 项匿名 HTTPS 已通过，包含 9 词修正版。微信真机验收仍待完成。标准与命令见 `VOICE-STANDARD.md`。

## 历史本地 Flo 参考库（已由 Emma 替代）

此前曾试听 B Flo 英式女声、115 词/分钟，以下保留受限本地库的历史记录。生成范围来自教材注册表：6 个单元、146 条关联、145 个独立词；同词跨单元共享文件。数据位于 `assets/local-audio/flo-en-gb-115-v1/`，使用独立 manifest，不进入小程序生成数据或资源分发仓库。

已生成并校验 145 份 WAV + 145 份 MP3，全部单元关联覆盖；MP3 总计 2,833,007 字节。本地 HTTP 音频响应与清单逐份匹配，原始 B 选型的三个基准词 WAV 保持逐字节一致。

本地 Flo 为 `preview / local-preview-only / pending`；Apple 系统音色的公开分发限制已记录。当前小程序已切换至 Emma resource:// 整词清单，Flo 不再由浏览器注入。以下表格为当前清单统计。

| 内容 | 数量 | 状态 |
| --- | ---: | --- |
| 传统英式音素 | 44（20 元音、24 辅音） | 结构完整；教学/发音审核 pending |
| PEP 2026 六上单元 | 6 | 官方目录检索佐证；词汇仍待实书核对 |
| 教材关联 | 146 | 来自公开词表重新整理，未取回原压缩包 |
| 独立 Word | 145 | run 跨单元共享，语境释义分别保留 |
| IPA | 145 条候选、0 条教学审核完成 | 6条有直接词典证据，139条AI草稿；开发版显示待核对标记 |
| 音节 / 拼写对齐 | 0 条已审核 | 6 个隔离候选；AI 对齐仍 pending |
| 音频资源位 | 189 | 145 条 Emma 整词试听已写入 resource（synthetic-preview、待发音核对）；44 条音素 missing；已推送，微信真机待验证 |
| 原创插画 | 1 | 已生成并引用，发布前检查形象及体积 |
| 原创图标源 | 4 | SVG 可编辑源 |

词表保留单词和短语，不纳入教材全文、插画、例句或第三方音频。未取得六下新版可核验内容，因此没有六下词库。旧对话的下载引用不代表文件已经可获取。

发布前优先核实：教材版次/单元词项与星号属性、专名拼写；read 等多音词、弱读、英美转写差异；音素说明与音频；每个素材来源和使用条件。本文是实际缺口记录，不是固定 AI 任务排期。

## 本轮开发后的实际可用性

收藏、自定义词、教材浏览、今日计数、翻卡复习已经实现。根据用户纠偏，145个教材词的详情/列表/复习卡显示待核对 IPA。音标渐变按真实媒体 currentTime 与逐词时间表推进，停止、切词及结束归零。145 词已完成机器逐项对齐；77 处低置信度内部边界使用模型时长插值，人工听审仍 pending，详见 `../artifacts/audio-sync-audit-2026-09-12/README.md`。浏览器和小程序使用相同的 Emma 清单；浏览器可显式读取本地 resource 做推送前预览，小程序通过远端下载。三类练习继续只使用已审核数据，因此当前为空态。

`npm run content:status` 输出内容计数，`node scripts/content-status.cjs --release` 会明确拒绝将当前内容认定为发布就绪。`learning-samples.json` 和 `audio-candidates.json` 不进入运行时打包；来源证据、结构通过和发音审核分别记录。
